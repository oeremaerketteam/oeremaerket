---
import { Image } from "astro:assets";
const { title, text, plakat, order } = Astro.props;
---

<section class="grid_plakat">
  <div class="phototext">
    <Image src={plakat} alt="plakat" class="plakat" />
  </div>
  <div class="content">
    <h2>{title}</h2>
    <p>{text}</p>
  </div>
</section>

<style define:vars={{ order }}>
  .plakat {
    max-width: 100%;
    width: 350px;
    height: auto;
  }
  .content {
    place-content: center;
    text-align: left;
    padding: 1rem;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.6rem;
    margin: 2rem;
  }
  .phototext {
    place-self: center;
    margin: 0;
  }
  @media screen and (width>800px) {
    .grid_plakat {
      display: grid;
      grid-template-columns: 1fr 1fr;
      margin: 5rem;
    }
    .content {
      order: var(--order);
    }
    .plakat {
      width: 450px;
    }
  }
</style>
